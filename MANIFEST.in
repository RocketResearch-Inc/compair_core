# Release artifacts contain the installable runtime only. Repository tests,
# specifications, operator docs, and validation helpers remain source-control
# assets and are verified from clean clones before distribution builds.
prune tests
prune docs
prune protocol
prune scripts

global-exclude *.py[cod]
global-exclude __pycache__
global-exclude .DS_Store
